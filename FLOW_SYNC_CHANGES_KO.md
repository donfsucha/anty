# D-LOGIS 버튼·상태 연동 수정본

## 적용 내용

1. 모든 운영 수치를 소수점 한 자리로 표시합니다.
2. 임무 목록과 상세 화면에서 드론명, 기체 ID, 모델을 함께 표시합니다.
3. 임무 상세 상단에 8단계 진행표시를 추가했습니다.
4. 주요 버튼을 누르면 단계별 처리창이 표시됩니다.
5. 운항 승인, 자동배정, 전체점검, 이륙, 일시대기, 재개, 긴급복귀, 배송완료, 착륙종료가 같은 작업엔진을 사용합니다.
6. 임무, 드론, 배터리, 지도, 작업이력, 명령이력을 동시에 갱신합니다.
7. 배송 완료와 기체 복귀·임무 종료를 분리했습니다.
8. 작업 완료 후 실행자, 처리시각, 결과가 임무 이력에 저장됩니다.

## 주요 버튼 흐름

- 운항 승인: 정보 검증 → 경로·화물 확인 → 승인자 기록 → 화면 동기화
- 자동배정: 대기 기체 조회 → 적재·정비 확인 → 배터리 확인 → 자원 예약 → 화면 동기화
- 전체점검: 6개 점검항목 순차 처리 → 점검자·시각 기록
- 이륙: 안전조건 → 명령 생성 → 전송 → ACK → 비행모드 → 텔레메트리 → 화면 동기화
- 긴급복귀: 안전여유 → 명령 생성 → 전송 → ACK → RTL → 복귀 ETA
- 배송완료: 배송지 → OTP → 화물함 → 좌표 → 증빙 → 복귀
- 착륙종료: 거점 도착 → 착륙 → Disarm → 자산 복원 → 종료시각 → 보고서 동기화

## 실행

```bash
python3 -m http.server 5500
```

Chrome 주소창:

```text
http://localhost:5500
```

기존 브라우저 캐시가 보이면 `Command + Shift + R`을 누르거나 시스템설정에서 데모 데이터를 초기화합니다.
