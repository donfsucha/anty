# D-LOGIS CONTROL V3 실운영형 가이드

## 1. V3 핵심 변경

D-LOGIS CONTROL V3는 단순한 관제 화면이 아니라 다음 업무가 하나의 Mission ID로 연결되도록 재구성한 버전입니다.

- 신규 배송임무 4단계 입력
- 운항 승인과 7개 비행 전 안전점검
- 이륙·일시대기·재개·긴급복귀·완료 명령 이력
- 드론 전송시각과 관제 수신시각 분리
- 위치·고도·속도·배터리·온도·통신·GNSS 기록
- 경보 발생값·기준값·확인자·확인시각 기록
- 배터리 SOC·SOH·온도·셀 편차·사이클 기록
- 배송 OTP·좌표·완료시각·온도·전자 인수증
- 감사로그와 9개 시트 XLSX 운영보고서
- 관제자·현장 조종자·배송 수령인별 화면

## 2. 소수점과 좌표 표시 규칙

고도, 속도, 거리, 화물중량, 배터리, 온도, 통신품질, 풍속, 셀 편차 등 운영 수치는 소수점 한 자리로 저장하고 표시합니다.

예시:

```text
81.238947 m  → 81.2 m
34.832659 km/h → 34.8 km/h
68.000032 % → 68.0 %
```

위도·경도는 한 자리로 반올림하면 위치 오차가 너무 커지므로 다음 원칙을 사용합니다.

- 관제 화면: 도·분·초(DMS) 표시, 초 단위 소수점 한 자리
- Excel 원본: WGS84 위도·경도 6자리

예시:

```text
37°30′12.4″N
126°45′58.7″E
```

## 3. Mac에서 실행

1. 압축을 풉니다.
2. VS Code에서 폴더를 엽니다.
3. `Terminal → New Terminal`을 선택합니다.
4. 아래 명령을 실행합니다.

```bash
python3 -m http.server 5500
```

5. Chrome 주소창에서 다음 주소를 엽니다.

```text
http://localhost:5500
```

`http://localhost:5500`은 터미널이 아니라 Chrome 주소창에 입력해야 합니다.

## 4. 화면별 사용 순서

### 관제센터

```text
관제현황
→ 조치 우선순위 확인
→ 승인 대기 임무 검토
→ 비행 전 점검
→ 이륙·운항 관제
→ 경보 조치
→ 운영기록 확인
→ XLSX 보고서 생성
```

### 현장 조종자

```text
오늘의 임무
→ 비행 전 점검
→ 이륙·임무 시작
→ 운항 상태 확인
→ 배송 완료
→ 복귀 확인
```

### 배송 수령인

```text
예상 도착시간
→ 진행률
→ 수령 OTP
→ 배송품목
→ 안전 수령 안내
```

## 5. Excel 운영보고서

관제현황 또는 운영기록 화면에서 `운영보고서 XLSX` 버튼을 누르면 실제 `.xlsx` 파일이 생성됩니다.

시트 구성:

1. `01_운영요약`
2. `02_임무목록`
3. `03_비행로그`
4. `04_명령이력`
5. `05_경보이력`
6. `06_배터리기록`
7. `07_배송증빙`
8. `08_비행전점검`
9. `09_감사로그`

날짜와 시간은 Excel 날짜 셀로 저장하며 `yyyy-mm-dd hh:mm:ss` 형식을 적용합니다. 운영 수치는 텍스트가 아닌 숫자 셀로 저장하고 소수점 한 자리 형식을 적용합니다.

Excel 생성을 위해 공식 SheetJS CDN을 사용하므로 인터넷 연결이 필요합니다.

## 6. 카카오맵 무료 모드 설정

V3는 카카오맵을 선택할 수 있으며, 키가 없거나 연결에 실패하면 OpenStreetMap으로 자동 전환합니다.

1. 카카오 개발자센터에서 앱을 생성합니다.
2. 카카오맵 API를 활성화합니다.
3. 플랫폼 키에서 JavaScript Key를 확인합니다.
4. JavaScript SDK 도메인에 다음 주소를 등록합니다.

로컬 실행:

```text
http://localhost:5500
```

Netlify 배포 후:

```text
https://발급받은이름.netlify.app
```

5. 앱에서 `시스템설정 → 지도 설정`으로 이동합니다.
6. JavaScript Key를 입력하고 `설정 저장·지도 다시 연결`을 누릅니다.

비용 방지 원칙:

- 카카오맵 무료 쿼터가 적용되는 앱만 사용
- Biz Wallet과 유료 API 사용을 연결하지 않음
- 무료 쿼터 대상이 아니거나 지도 로딩 실패 시 OSM으로 자동 전환
- Google Maps는 결제계정과 과금 관리가 필요하므로 기본 제공자에서 제외

## 7. Netlify 배포

GitHub Pages 설정 없이도 배포할 수 있습니다.

1. 이 브랜치 ZIP을 다운로드합니다.
2. ZIP을 더블클릭하여 압축을 풉니다.
3. 폴더를 열어 `index.html`이 바로 보이는지 확인합니다.
4. 해당 폴더 전체를 Netlify Drop에 끌어 놓습니다.
5. 발급된 `netlify.app` 주소를 카카오 JavaScript SDK 도메인에 등록합니다.

## 8. 데이터 저장 구조

현재 V3는 다음 두 저장소를 함께 사용합니다.

- `localStorage`: 임무·기체·배터리·화면 설정과 최근 상태
- `IndexedDB`: 텔레메트리·명령·점검·배터리·경보·증빙·감사로그

시뮬레이션 텔레메트리는 기본 5초 간격으로 생성됩니다. 시스템설정에서 5초, 10초, 30초 중 선택할 수 있습니다.

## 9. 실제 드론 연결 전 필수 개발

V3는 정밀 시연과 업무검증이 가능한 브라우저 버전입니다. 실제 다중 사용자 상용 운항에는 다음 구성이 추가되어야 합니다.

- 계정·권한·로그인 서버
- PostgreSQL 등 중앙 데이터베이스
- 서버 수신시각과 NTP 시간동기화
- MAVLink 또는 제조사 SDK 게이트웨이
- 명령 ACK와 실제 비행모드 재확인
- 배송사진·서명·파일 저장소
- 서버 백업·보존기간·암호화·감사로그
- 기상·공역·비행승인 데이터 연동

실제 드론의 모터·이륙·복귀 제어는 기체, 비행컨트롤러, 통신장치, 운항승인 절차와 현장 안전검증을 완료한 뒤 활성화해야 합니다.
